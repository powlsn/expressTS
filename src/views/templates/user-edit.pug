extends layout

block content
    .container
        .col-md-4.text-justify
            h1.main-title #{title}
            form(action=`/user/${user.id}/edit` id="edit-form")
                input(id="data-id" class="form-control" type="hidden" name="data-id" value=`${user.id}`)
                label(for="name") Nickname
                input(id="name" class="form-control" type="text" name="name" aria-description="nameHelp" value=`${user.name}`)
                label(for="firstname") Firstname
                input(id="firstname" class="form-control" type="text" name="firstname" aria-description="firstnameHelp" value=`${user.firstname}`)
                label(for="lastname") Lastname
                input(id="lastname" class="form-control" type="text" name="lastname" aria-description="lastnameHelp" value=`${user.lastname}`)
                button(type="submit") Submit


block footerScripts
    script
        include script.js